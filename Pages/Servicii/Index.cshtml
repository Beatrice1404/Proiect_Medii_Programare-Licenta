@page
@model Proiect_Medii_Programare.Pages.Servicii.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Servicii Oferite</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Serviciu[0].ServiciiSuplimentare)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Serviciu[0].PrețServiciu)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Serviciu) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ServiciiSuplimentare)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PrețServiciu)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.ID">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
